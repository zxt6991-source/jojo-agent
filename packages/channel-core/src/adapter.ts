import type {
  ChannelAdapter,
  ChannelAdapterContext,
  ChannelAdapterFactory,
  ChannelSecretResolver
} from './internal-types.js';

export type { ChannelAdapter, ChannelAdapterContext, ChannelAdapterFactory, ChannelSecretResolver };
